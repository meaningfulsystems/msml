# Codex Session: MSML v1.0 Model / Diagram Split

**Date:** 2026-05-17  
**Agent:** codex  
**Session file:** `codex-20260517-1702-session.md`  

## Objective

Implement the major MSML v1.0 update:

- `.msml` files become semantic model files.
- `.msmd` files become diagram/view files.
- Diagrams reference model definitions and relationships.
- Renderer loads model files and resolves `model_ref` / `relationship_ref`.
- Documentation and blog links are updated.
- Existing diagrams render successfully after migration.

## Log

- 17:02 Started session after GitHub checkpoint `0665c36`.
- 17:03 Began renderer update for model-backed diagrams.
- 17:04 Andrew clarified there should be no backwards compatibility: `.msml` is model-only, `.msmd` is diagram-only, renderer/render_all should target the new design only.
- 17:05 Updated `render_msml.py` and `render_all.py` for new-only `.msmd` rendering with `.msml` model loading.
- 17:06 Andrew paused implementation to put the specification first. Renamed `msml-requirements.md` to `msml-specification.md` and rewrote it for v1.0 `.msml` model / `.msmd` diagram split.
- 17:10 Migrated HOS and toaster diagram files from diagram-style `.msml` to `.msmd`, and created `hos-model.msml` and `toaster-model.msml`.
- 17:12 Reworked generated model relationship IDs to be globally unique and rerendered both project folders successfully.
- 17:15 Updated README, operational concept links, and the implementation plan to describe only the v1.0 model/view split.
- 17:18 Verified failure behavior for missing model files, unknown `model_ref` values, and direct `.msml` rendering.
- 17:22 Updated visible feedback notes to point at `msml-specification.md` and `.msmd` diagram files. Re-ran all HOS and toaster renders successfully.
