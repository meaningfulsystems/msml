# Codex Session: Claude Plan Review

**Date:** 2026-05-17  
**Agent:** codex  
**Session file:** `codex-20260517-2119-session.md`

## Objective

Review `ai-collab/claude-codex-plan_20260517-2113.md` and decide whether Codex agrees with it. If not, update the plan and record notes in a new session file.

## Review Notes

I agree with the plan's broad direction:

- Move to `model_files` only.
- Add missing toaster parametric definitions.
- Remove duplicate `Toaster.User` in favor of `Toaster.Actor.User`.
- Add `subject_ref` to IBD/parametric diagrams.
- Improve renderer error messages.
- Add a lightweight CLI validator because Andrew explicitly asked for validation with useful CLI error output.

I do not agree with the original plan as written in three places:

1. It said all decisions were resolved. They are not. Andrew asked for clarification on HD1 and HD4, both of which affect whether notation-only diagram nodes may remain outside the model.
2. It blurred renderer and validator responsibilities. The renderer should hard-fail for bad file shape, old `model_file`, missing model files, and unresolved refs. It should not make absent `model_ref` on model-mandatory elements a default render failure; that belongs in validator strict mode.
3. It told Codex to embed/attach PNG confirmation in the session log. The session log should record the observation, not embed binary artifacts.

## Plan Updates Made

- Changed plan status to "Codex-reviewed draft" rather than "all decisions resolved."
- Added section `0.1 Codex Disagreement / Required Clarification`.
- Added C1 explaining the difference between semantic completeness and notation-only diagram mechanics.
- Added C2 elaborating activity-flow options for Andrew.
- Marked Phase 1.1 and related strict inline-relationship checks as blocked until C1/C2 are resolved.
- Updated fail-loud wording so renderer errors and validator errors are distinct.
- Changed renderer guidance to raise `ValueError` inside `render()` and let CLI `main()` exit non-zero.
- Clarified that unresolved `model_ref` / `relationship_ref` is a hard renderer error, while absent `model_ref` on mandatory model element types is a validator strict-mode error.
- Moved `model_file` singular usage to a schema-level error because Andrew said to enforce `model_files` only.
- Adjusted the testing checklist to expect `MSML-SCHEMA-009` for `model_file`.
- Updated the "What NOT to Change" section to allow regenerated PNGs when source changes and to allow append-only session logging.

## Current Recommendation

Before implementation, Andrew should answer C1 and C2 in the plan. My recommendation is:

- C1: the model is complete for system semantics, while diagram notation mechanics may remain diagram-local in v1.0.
- C2: choose Option C, which keeps v1.0 simple but documents the future path toward a complete activity graph model if needed.

## Andrew Follow-Up

Andrew answered C1/C2 in the plan:

- C1: `.msml` should be the complete source of truth. `.msmd` is a view of the model, and the diagrams should be recreatable from a working `.msml` model plus view/layout files.
- C2: choose Option B. The full activity graph, including initial/final/fork/join/decision/merge nodes and flows, belongs in the model.

Andrew also clarified a model hierarchy direction: child/specialized models can reference parent models so the whole system does not have to live in one large file. Example: `pacific-ocean.msml` imports or references `oceans.msml`, which imports or references `earth.msml`.

## Plan Updates After Andrew's Comments

- Changed the plan status to reflect that Andrew resolved HD1/HD4 toward complete model source of truth.
- Replaced the old inline-relationship rule with a stricter rule: every current repository diagram relationship should have `relationship_ref`.
- Added spec work for model definition types that were previously treated as notation-only: initial/final nodes, fork/join, decision/merge, pseudostates, execution occurrences, ports, and optional comments/annotations.
- Added spec clarification for parent/child model imports.
- Added model-file work to create definitions for all current diagram-only elements.
- Added model-file work to create relationships for all current inline diagram relationships.
- Added diagram-file work to put `model_ref` on every element and `relationship_ref` on every relationship.
- Updated renderer and validator expectations so missing `model_ref` / `relationship_ref` are schema-level errors in the v1.0 repository format.

## Updated Recommendation

I accept Andrew's direction. It is larger than my initial weekend-scope recommendation, but it is more coherent as a modeling language: `.msml` is the model graph, `.msmd` is strictly a rendered view. The next implementation pass should treat model completeness as a hard invariant for repository examples.
